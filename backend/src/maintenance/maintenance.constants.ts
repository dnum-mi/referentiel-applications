export const MAINTENANCE_MESSAGE =
  "Le site est actuellement en maintenance. Les données restent disponibles en lecture seule.";

export const READ_ONLY_HTTP_METHODS = new Set(["GET", "HEAD", "OPTIONS"]);
