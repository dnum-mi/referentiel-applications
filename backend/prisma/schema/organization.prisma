model Organization {
  id       String         @id @default(uuid())
  label    String
  url      String?
  sigle    String?
  parentId String?
  parent   Organization?  @relation("OrganizationParent", fields: [parentId], references: [id])
  children Organization[] @relation("OrganizationParent")
  users    User[]
  actors   Actor[]
}
