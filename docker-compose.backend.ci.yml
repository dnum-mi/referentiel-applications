services:
  backend:
    volumes:
      - ./coverage:/app/coverage

  postgres:
    healthcheck:
      test: [CMD-SHELL, pg_isready, -U, postgres]
      interval: 5s
      timeout: 2s
      retries: 10
