services:
  backend:
    volumes:
      - ./backend/coverage:/app/coverage
      - /app/node_modules
  postgres:
    healthcheck:
      test: [CMD-SHELL, pg_isready, -U, postgres]
      interval: 5s
      timeout: 2s
      retries: 10
