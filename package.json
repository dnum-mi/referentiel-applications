{
  "name": "referentiel-applications",
  "type": "module",
  "version": "1.33.2",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "format": "eslint . --fix",
    "lint": "eslint .",
    "prepare": "husky",
    "commitlint": "commitlint --edit",
    "up": "docker compose up --build",
    "down": "docker compose down -v",
    "generate": "cd backend && pnpm db:generate && cd ../frontend && pnpm api:generate",
    "docker:init": "docker compose exec backend pnpm db:init && docker compose restart backend && docker compose exec client pnpm api:generate",
    "npm:install": "pnpm install && cd backend && pnpm install && cd ../frontend && pnpm install"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^5.2.0",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@eslint/js": "^9.33.0",
    "eslint": "^9.33.0",
    "eslint-plugin-vue": "^10.4.0",
    "globals": "^16.3.0",
    "husky": "^9.1.7",
    "typescript-eslint": "^8.39.0"
  }
}
